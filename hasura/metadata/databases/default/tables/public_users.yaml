table:
  name: users
  schema: public
array_relationships:
  - name: project_users
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: project_users
          schema: public
  - name: records
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: records
          schema: public
  - name: teachers
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: teachers
          schema: public
  - name: user_details
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: user_details
          schema: public
  - name: user_skills
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: user_skills
          schema: public
