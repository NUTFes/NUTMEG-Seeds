table:
  name: chapters
  schema: public
object_relationships:
  - name: curriculum
    using:
      foreign_key_constraint_on: curriculum_id
array_relationships:
  - name: records
    using:
      foreign_key_constraint_on:
        column: chapter_id
        table:
          name: records
          schema: public
